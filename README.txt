
레미콘 통합 계산기 (Supabase 연동 버전)

파일 구성
- index.html : 홈
- dispatch.html : 출하량 계산기
- cost.html : 원가 계산기
- admin.html : 관리자 관리
- app.js : 공통 로직 + Supabase 연결
- style.css : 공통 스타일

1. app.js 상단의 값을 본인 Supabase 프로젝트 값으로 바꾸세요.
   const SUPABASE_URL = "YOUR_SUPABASE_URL";
   const SUPABASE_ANON_KEY = "YOUR_SUPABASE_ANON_KEY";

2. Supabase에는 아래 3개 테이블이 있어야 합니다.
   - mixes
   - prices
   - dispatch_settings

3. RLS를 끄거나 정책을 허용해야 웹에서 읽고 쓸 수 있습니다.

4. GitHub Pages에 올리면 홈 주소는 index.html 입니다.

[260310_022700 업데이트]
- cost 관리자 수정 영역을 배합비 / 단가 2개 섹션으로 분리
- 배합비 섹션에 좌측 name/code 리스트 + 우측 상세 입력 폼 적용
- 배합비 섹션에 추가 / 삭제 / 저장 버튼 적용
- 단가 섹션에 별도 저장 버튼 적용
- 저장 버튼 및 주요 활성 버튼을 파란색 스타일로 변경
- O_PRICE 필드를 단가 영역에서 제거하고 배합비 입력 항목으로 이동
- 선택 배합의 O_PRICE가 있으면 판매단가 기본값에 반영되도록 조정
- 배합 저장은 mixes.id 기준으로 처리하도록 변경


- 260310_041446: 원가계산기 입력 영역에 정가(O_PRICE) 표시를 추가하고, 판매단가 | 정가 2열 배치로 조정함. 정가는 선택한 배합의 mixes.o_price 값을 읽어 표시하도록 반영.

- 260310_042727: 손익 요약 영역을 총매출 / 총 원가 / 예상 이익 / 마진율 | 할인율 배치로 변경함. 할인율은 선택 배합의 정가(O_PRICE) 대비 판매단가 기준으로 계산하고, 음수는 0% 아래로 내려가지 않도록 처리함.


[260310_043210 업데이트]
- 손익 요약 레이아웃을 모든 화면 크기에서 동일하게 유지하도록 조정
- 배치: 총매출 / 총 원가 / 예상 이익 / 마진율 | 할인율
- 작은 화면에서도 마지막 줄만 2열로 유지되도록 summary-grid 고정


260310_051530
- cost 입력 영역의 판매단가 / 정가 배치를 시안처럼 각각 개별 라벨 + 입력칸 구조로 조정
- dispatch 입력의 왕복시간 / 상세설정 영역과 상세패널 사이 간격을 다른 필드와 비슷하게 축소
- 전체 결과값과 주요 수치의 폰트 두께를 heavy bold에서 bold 수준으로 완화


260310_060215
- 관리자 원자재 단가 라벨을 코드만 표시하던 방식에서 부가명 표시 가능 구조로 변경
- 배합비 편집 영역에도 동일한 라벨 규칙을 적용해 C1 / C2 등의 항목에 부가명을 함께 표시 가능
- 배합비 섹션 제목 옆에 이름 표시 토글 버튼을 추가해 켜면 C1(시멘트1종), 끄면 C1만 보이도록 반영

260310_061200
- 패키지 재정리본 생성
- 관리자 원자재 단가 영역과 배합비 데이터 입력 영역에 재료 부가명 라벨 구조 유지
- 배합비 제목 옆 토글 버튼으로 C1(시멘트1종) / C1 표시 전환 가능
